/** Static class maps — scan-first path (no dynamic shade calls). */
export const STATIC_MAP = {
  card: "rounded-lg border border-neutral-200 bg-white p-4",
  badge: "inline-flex items-center text-xs font-medium text-brand-600",
} as const;
